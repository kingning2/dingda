name: Build Locked Desktop

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows-x64-locked:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install uv
        run: pip install uv

      - name: Install Perl (vendored OpenSSL)
        shell: pwsh
        run: |
          choco install strawberryperl -y --no-progress
          "C:\Strawberry\perl\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          "C:\Strawberry\c\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          perl -v

      # release profile 不会为 externalBin 生成 stub；需先冻结真实 sidecar
      - name: Build frozen sidecar
        run: node tooling/scripts/build-sidecar.mjs --target x86_64-pc-windows-msvc

      - name: Build locked Windows x64 bundle
        run: pnpm tauri build locked --target x86_64-pc-windows-msvc

      - name: Verify bundled license-verifier
        shell: pwsh
        run: |
          # Tauri copies external bins to release/ (not bundle/). Installers only contain msi/nsis.
          $candidates = @(
            "apps/desktop/src-tauri/binaries/license-verifier-x86_64-pc-windows-msvc.exe",
            "target/x86_64-pc-windows-msvc/release/license-verifier.exe",
            "apps/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/license-verifier.exe"
          )
          $found = $false
          foreach ($path in $candidates) {
            if (Test-Path $path) {
              $found = $true
              Write-Host "Found verifier: $path"
              break
            }
          }
          if (-not $found) {
            Write-Error "license-verifier not found. Checked:`n$($candidates -join "`n")"
            exit 1
          }
          $bundleRoots = @(
            "target/x86_64-pc-windows-msvc/release/bundle",
            "apps/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle"
          )
          $bundleDir = $bundleRoots | Where-Object { Test-Path $_ } | Select-Object -First 1
          if (-not $bundleDir) {
            Write-Error "Bundle directory not found"
            exit 1
          }
          Write-Host "Bundle output: $bundleDir"

      - uses: actions/upload-artifact@v4
        with:
          name: windows-x64-locked-build-log
          path: |
            apps/desktop/src-tauri/binaries/license-verifier-x86_64-pc-windows-msvc.exe
            apps/desktop/src-tauri/generated/license_verifier.sha256
          if-no-files-found: error
