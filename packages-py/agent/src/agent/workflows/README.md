# agent/workflows

多步业务图（调研、以后比价）。只组合 Tool 名，不碰浏览器引擎。

## 本目录文件

### `research.py`

`ResearchWorkflow`：**骨架**。计划：多次 `search`/`product` 再归纳。由未来的 `api/research` 调用。

### `__init__.py`

包标记。

## 子目录

无。
