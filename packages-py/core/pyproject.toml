[project]
name = "core"
version = "0.1.0"
description = "DingDa 底座：配置 / 日志 / 全局错误。不认识任何业务域。"
requires-python = ">=3.13"
dependencies = [
  "python-dotenv>=1.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
