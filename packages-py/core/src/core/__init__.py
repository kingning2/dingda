"""应用内核包。

本包存放与具体业务无关、但整个后端都依赖的基础能力：

模块说明：
    - ``config``   : 从环境变量/命令行读取 host、port、日志级别等配置
    - ``logging``  : Rich 彩色控制台日志初始化
    ``lifespan`` : FastAPI 启动/关闭时的钩子（日志、数据库等）

这些模块在应用启动最早阶段被调用，不依赖 ``domains`` 或 ``api`` 层。
"""
