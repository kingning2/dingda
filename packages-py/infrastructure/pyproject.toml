[project]
name = "infrastructure"
version = "0.1.0"
description = "DingDa 进程级基建：产品 SQLite 与 EventBus。不是业务层。"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
