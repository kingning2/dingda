{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "dingda://copilot/run_start.response/v1",
  "title": "copilot/run_start.response",
  "type": "object",
  "description": "POST /v1/copilot/agui 的响应不是 JSON，而是 text/event-stream：每帧 `data: {agui_event}`（见 agui_event.schema.json），RUN_FINISHED/RUN_ERROR 或客户端断开后结束。",
  "required": ["content_type"],
  "properties": {
    "content_type": { "const": "text/event-stream" },
    "frames": {
      "type": "array",
      "items": { "$ref": "agui_event.schema.json" }
    }
  },
  "additionalProperties": false
}
