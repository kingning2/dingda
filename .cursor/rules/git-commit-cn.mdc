---
description: 在用户要求创建 git 提交、写中文 commit message、按项目 Conventional Commits 规范提交，或 @git-commit-cn 时应用；规定 type/scope/中文简述、按模块拆分多个 commit、Husky 校验与提交流程。
alwaysApply: false
---
# Git 提交规范（中文）

**在用户要求提交代码、按本规范写 message，或 @ 提及 `git-commit-cn` 时执行。**

## 提交信息格式

```
<type>(<scope>): <中文简述>
```

- **type**（必填）：`feat` | `fix` | `docs` | `chore` | `refactor` | `perf` | `test` | `build` | `style`
- **scope**（推荐）：`tauri` | `frontend` | `ui` | `animation` | `build` | `deps` | `env` | `data` | `skills` 等，与改动模块一致
- **简述**：简体中文，一句话说清「为什么 / 做了什么」，不用英文句子

### 示例（与本仓库历史一致）

```
feat(ui): 重构下载进度、工具卡片与标题栏
feat(tauri): 支持 downloadKey 解析与 Windows zip 分步安装
chore(deps): 增加动画相关依赖
docs: 补充工具清单字段说明
```

## 自动化校验（Husky）

- **pre-commit**：先执行 `pnpm run format`（Prettier + `cargo fmt`），将格式化后的已跟踪文件 `git add -u` 回暂存区，再执行 `pnpm run lint`（全量 ESLint）；任一步失败则无法提交
- **commit-msg**：由 `@commitlint/config-conventional` 校验，message 必须以 `feat` / `fix` / `chore` 等 type 开头，格式为 `<type>(<scope>): <简述>`

跳过校验需显式使用 `git commit --no-verify`（默认禁止）。

## 分模块提交（默认，必遵）

**不要把所有改动塞进一个 commit。** 除非用户明确要求「单次提交 / squash / 一个 commit」，否则按**模块边界**拆成多个 commit，每个 commit 只包含同一模块、同一目的的变更。

### 模块划分（与本仓库目录对应）

| 模块 | 典型路径 / 范围 | 推荐 scope |
|------|-----------------|------------|
| 构建与依赖 | 根 `package.json`、`Cargo.toml`、`pnpm-workspace.yaml`、`tsconfig*.json`、`scripts/` | `build` / `deps` |
| 环境与配置 | `.gitignore`、`.env.example`、`components.json` | `env` / `chore` |
| Rust 侧 | `packages-rs/*/src/`（Cargo workspace，成员见根 `Cargo.toml`） | `rust` |
| Python Server | `server/src/` | `server` |
| 前端线协议 | `packages/contracts/src/` | `contracts` |
| 前端基座 | `packages/client/{app-state,runtime,routes}/src/` | `frontend` |
| 前端 UI 包 | `packages/client/ui-*/src/` | `ui` |
| 应用装配 | `apps/web/src/`（含 `boot/` 启动编排） | `web` |
| 规则与文档 | `AGENTS.md`、`README.md`、`packages/**/README.md`、`.agents/`、`.cursor/` | `docs` |

跨模块功能应拆成例如：`feat(server)` 后端与契约 → `feat(contracts)` 线协议 → `feat(ui)` 组件 → `feat(web)` 装配 → `docs` 说明，**而不是**一条 `feat: 全部搞定`。

注意：**前端拆包后没有仓库根 `src/`**。见到 `src/contracts/`、`src/lib/`、`src/components/` 这类路径一律是旧 OpenDesk 残留，应改写为 `packages/contracts/src/`、`packages/client/runtime/src/`、`packages/client/ui-*/src/`。

### 拆分原则

- 一个 commit = 一个可独立 review 的主题；改了多少文件不重要，**边界**才重要。
- 先提交底层（依赖、Rust、sidecar），再提交消费方（前端、文档），减少中间态无法编译的提交（若无法避免，在 commit message 简述中注明依赖顺序）。
- 仅当改动极小且天然同属一处（如只改一个组件里的 typo）才可单 commit。
- 拆完后向用户简要列出：commit 数量、每条 message、各自包含的路径范围。

## 执行提交时的流程

1. 并行查看：`git status`、`git diff`、`git log -10 --oneline`（对齐既有风格）
2. 按上文**模块划分**将变更分组；为每组拟一条 `<type>(<scope>): <中文简述>`
3. **不要**提交 `.env`、密钥、token 等敏感文件；若用户坚持，先警告
4. **按组循环**：`git add <该组路径>` → `git commit -m "..."` → 确认成功；全部组完成后再 `git status`
5. 未要求时 **不要** `git push`

## 禁止

- 英文为主的 commit message（如 `Add modal window`）
- 无 type 的随意描述（如 `update`、`fix bug`）
- 跳过 hooks（`--no-verify`）除非用户明确要求
- **默认禁止**把多模块、多主题改动合并为单个 commit（用户明确要求除外）
